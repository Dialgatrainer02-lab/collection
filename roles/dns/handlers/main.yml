- name: Restart dns
  community.docker.docker_compose_v2:
    project_src: "{{ config_dir }}"
    state: restarted
- name: Restart docker
  ansible.builtin.systemd_service:
    name: docker
    state: restarted